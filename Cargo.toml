[package]
name = "subculture-save-editor"
version = "1.1.0"
edition = "2024"

[dependencies]
eframe = "0.24"
egui = "0.24"
rfd = "0.12"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser", "windef", "winbase"] }
